# A股多策略政府决策系统实施方案 V6

## 0. 文档定位

本文件是本项目后续开发、测试、回测、策略准入、模拟运行和正式验收的统一基线。

它不是策略宣传材料，也不是根据一次回测结果反向编写的说明。系统中的任何策略、模型或参数，只有通过本文规定的点时数据、执行真实性、样本外验证、容量和风险门禁后，才允许进入正式组合。

在 Phase 0 数据审计和 Phase 1 回测引擎验证全部通过前：

- 所有绩效结果必须标记为“探索性结果”。
- 所有图表必须显示“基于未完全验证数据，禁止引用为实盘期望收益”。
- 自动准入模块必须强制返回 `FAIL`。
- 不允许通过人工开关、配置覆盖或手工修改结果文件绕过门禁。

---

## 1. 最终建设目标

系统需要证明以下事实，而不是单纯证明历史收益曲线好看：

1. 决策使用的数据在当时真实可得，不存在未来函数。
2. 指数成分、公司行为、停牌和财报等数据具有正确的生效时点。
3. 每个策略信号具有清晰、可复现、可审计的定义。
4. 信号在独立事件口径下拥有足够的有效样本。
5. 信号在严格样本外窗口内仍有统计有效性。
6. 收益在佣金、印花税、滑点、冲击成本和不可成交约束后仍为正。
7. 胜率、盈亏比和凯利仓位经过收缩和下置信界处理。
8. 多策略冲突、相关性和重复暴露得到明确处理。
9. 股票池、行业、单票、流动性和容量约束能够执行。
10. 股票掉出优质池后，不机械卖出，而由持仓凯利、收益预期和风险状态决定。
11. 系统能够解释每一笔买入、加仓、减仓、继续持有和退出。
12. 运行中出现数据异常、模型漂移或执行失败时能够自动降级。

最终流程固定为：

```text
点时数据和可交易股票池
→ 指标与独立策略事件
→ 历史统计和受限ML校准
→ 组内去重与跨组冲突聚合
→ 贝叶斯下置信界半凯利
→ 政府组合风险控制
→ 真实成交模拟与持仓管理
→ 绩效归因、准入和持续监控
```

系统必须坚持三项权力边界：

- 策略组负责提供证据，不直接决定最终仓位。
- 机器学习负责校准和挑战，不直接控制仓位。
- 政府层负责组合风险约束，不任意篡改单个策略分数。

凯利结果是单票目标仓位的主要评分，其他指标只作为输入证据、硬约束或风险辅助。

---

## 2. 架构取舍

不采用过度复杂的七层或多级模型堆叠。正式架构压缩为四个业务层和两个基础设施层。

### 2.1 基础设施层 A：点时数据层

职责：

- 通达信日线读取。
- 复权研究价格与真实成交价格双视图。
- 历史指数成分。
- 交易日历。
- 停复牌和涨跌停状态。
- 公司行为账本。
- 财报和事件的公告时点。
- 数据版本、来源和质量状态。

该层只回答“当时能够知道什么”和“当时能否成交”，不产生策略判断。

### 2.2 基础设施层 B：回测与执行层

职责：

- T+1。
- 整数手。
- 佣金、印花税、过户费。
- 滑点与冲击成本。
- 部分成交。
- 涨停买不到、跌停卖不出。
- 停牌冻结。
- 持久订单。
- 现金、分红和公司行为账本。

该层必须独立于策略，任何策略都不能绕过执行约束。

### 2.3 业务层 1：策略证据层

职责：

- 计算指标。
- 生成独立策略事件。
- 输出标准化 `StrategySignal`。
- 提供方向、强度、预测周期、建议退出条件和数据版本。

策略层不得直接输出最终目标仓位。

### 2.4 业务层 2：统计与校准层

职责：

- 生成成熟标签。
- 统计独立事件胜率和盈亏比。
- 贝叶斯收缩。
- 概率校准。
- 受限 ML 挑战。
- 组内去重与策略冲突处理。

### 2.5 业务层 3：仓位管理层

职责：

- 计算 `p_win`、`p_loss`、`payoff_ratio`。
- 使用下置信界计算保守凯利。
- 生成单票目标权重。
- 决定加仓、维持、部分减仓、全部退出。
- 维护掉池后继续持仓和分级退出逻辑。

原“安全组”在语义和代码上统一改为“仓位管理组”。如旧代码仍依赖 `SafetyDecision`，只允许通过兼容适配器保留，不得让旧安全分数继续控制主排序。

### 2.6 业务层 4：政府组合控制层

职责：

- 输出市场风险折扣。
- 控制组合总仓位上限。
- 控制行业、策略组和风险暴露。
- 决定是否暂停新开仓。
- 在风险危机时触发组合级去杠杆。

政府层不得直接将某个股票从低凯利改成高凯利，也不得因为主观判断给某策略临时加分。

---

## 3. 数据门禁与数据白皮书

### 3.1 数据源抽象

建立统一 `MarketDataProvider` 接口，隔离具体数据源：

```text
get_daily_bars()
get_trade_calendar()
get_suspensions()
get_limit_status()
get_adjustment_factors()
get_corporate_actions()
get_index_constituents()
get_index_total_return()
get_event_announcements()
```

通达信负责本地基础行情。Baostock、AkShare、Tushare、官方 CSV 或其他来源只能作为具体适配器。任何策略模块不得直接调用某个外部数据源。

每条外部数据至少记录：

```text
source_name
source_version
fetch_timestamp
effective_date
announcement_timestamp
available_timestamp
raw_file_hash
quality_status
```

### 3.2 数据完整性白皮书

Phase 0 必须生成机器可读和人工可读两份白皮书，逐字段说明：

- 字段业务含义。
- 原始来源。
- 起止日期。
- 缺失比例。
- 异常比例。
- 是否点时。
- 更新时间。
- 是否经过填补。
- 填补方法。
- 已知误差。
- 允许使用的模块。
- 失败时的降级规则。

### 3.3 严格的时间字段

系统至少区分：

```text
event_timestamp        事件实际发生时间
announcement_timestamp 对外公告时间
available_timestamp    系统最早可读取时间
signal_timestamp       策略信号生成时间
tradeable_timestamp    最早允许成交时间
reference_date         标签完全成熟日期
```

历史统计只能使用：

```text
reference_date < current_decision_date
```

若数据在当前决策日盘中才到达，即使自然日期相同，也不得用于当日开盘或此前的决策。

每次统计必须记录：

- 因标签未成熟排除的事件数。
- 因数据到达时间不合格排除的事件数。
- 因数据版本不一致排除的事件数。

### 3.4 股票池

基础股票池固定为：

- 沪深300。
- 中证500。
- 中证A500。

必须使用历史点时成分，不允许使用当前成分回溯历史。

成分表建议字段：

```text
index_code
symbol
announcement_date
effective_date
end_date
source
source_version
is_verified
```

股票池生成顺序：

1. 根据决策日获取已生效指数成分。
2. 取三个指数的并集并按 `symbol` 去重。
3. 只保留 A 股普通股票。
4. 剔除 ST、*ST、退市整理期。
5. 剔除上市历史不足阈值的股票。
6. 剔除数据不完整的股票。
7. 标记停牌和不可成交状态。
8. 计算 ADV20、Amihud 和异常跳变。
9. 执行绝对流动性与容量过滤。
10. 保留过滤原因和每步剩余数量。

新调入指数成分必须等待至少 5 个交易日，并获得有效 ADV 数据后才允许新买入。

历史 A500 成分无法可靠获取时：

- A500 分支标记 `DATA_UNVERIFIED`。
- 不静默使用当前成分代替历史成分。
- 正式准入只使用已验证指数池。
- 报告明确列出缺失区间和受影响回测。

### 3.5 可交易性

日频研究与实盘判断必须分开：

- 日频回测只能用 OHLCV 近似整日封板。
- 分钟级实盘必须检查实时盘口和临时停牌。
- “收盘涨停”不必然等于“全天无法买入”。
- “收盘跌停”不必然等于“全天无法卖出”。

近似规则必须保守，并在报告中列明误差方向。

---

## 4. 指标因子体系

指标不是独立策略。每个指标必须有明确消费者，避免同一信息重复计权。

### 4.1 基础指标

必须统一实现并登记：

- 收益：1、5、10、20、60 日收益。
- 均线：MA5、MA10、MA20、MA60、MA120。
- 指数均线：EMA20。
- RSI：RSI6、RSI14、RSI24。
- MACD：DIF、DEA、Histogram、金叉、死叉、零轴位置、柱体变化率。
- KDJ：K、D、J、超买超卖、金叉死叉。
- BOLL：中轨、上轨、下轨、带宽、价格位置。
- VOL：成交量均值、量比、成交额均值、成交额比率。
- ATR：ATR14、ATR20、标准化 ATR。
- 波动率：10、20、60 日。
- 趋势：海龟 20/55 日突破。
- 均值回归：均线偏离 z-score、回归半衰期。
- K 线：实体、上下影线、振幅、尾盘强度日线代理。
- 流动性：ADV20、Amihud、零成交天数。
- 市场状态：指数趋势、波动率分位数、市场宽度。

### 4.2 因子注册表

每个因子必须在注册表中声明：

```text
factor_id
version
required_columns
lookback_days
output_columns
consumer_modules
point_in_time_safe
missing_policy
normalization_method
```

未声明消费者的因子不能进入模型。未通过未来信息审计的因子不能进入特征表正式版本。

### 4.3 标准化

截面因子只能使用当日可用股票做 winsorize 和标准化。禁止使用全历史均值或未来横截面。

建议：

- 连续因子先进行 1%/99% 截尾。
- 再按行业中性或全市场 robust z-score。
- 离散事件因子保留 0/1 或有序状态。
- 缺失值增加缺失标记，不轻易用全样本均值填充。

---

## 5. 第一版正式策略与观察策略

### 5.1 正式候选策略组

第一版只选择四类低相关代表，避免策略数量超过有效样本能力。

#### 趋势组：MACD 金叉

冻结结构：

- MACD Histogram 从非正转正。
- 同一轮持续为正只记录第一次事件。
- 可增加价格位于 EMA20 或 MA20 上方的趋势过滤。

建议初始参数：

```text
fast=12
slow=26
signal=9
target_horizon=10日
max_holding_days=20
cooldown_days=5
```

允许有限搜索：

- 快线 10–14。
- 慢线 24–30。
- 信号线 7–10。
- 持有期 5、10、15、20 日。

#### 反转组：RSI 超卖企稳

冻结结构：

- RSI 进入超卖区。
- RSI 不再继续恶化或向上穿回阈值。
- 排除明显单边下跌和不可成交股票。

建议初始参数：

```text
rsi_period=14
oversold_threshold=30
target_horizon=5日
max_holding_days=10
cooldown_days=3
```

允许有限搜索：

- RSI 周期 6、14、24。
- 阈值 25、30、35。
- 持有期 3、5、10 日。

#### 量价组：缩量回调

冻结结构：

- 中期趋势仍为正。
- 价格从短期高点回调。
- 成交量低于近期均量。
- 不在明显破位状态。

建议初始参数：

```text
trend_ma=20
pullback_window=5
volume_ratio_max=0.8
target_horizon=5日
max_holding_days=10
cooldown_days=3
```

允许有限搜索：

- 趋势均线 20、30、60。
- 回调幅度。
- 量比阈值 0.6–0.9。
- 持有期 5、10、15 日。

#### 事件组：涨停后续

冻结结构：

- 前一交易日涨停。
- 不假设涨停板上可以买到。
- 下一交易日开盘后才允许交易。
- 连续涨停序列按一个事件簇处理。

建议初始参数：

```text
target_horizon=2日
max_holding_days=5
cooldown_days=0
cluster_consecutive_limits=true
```

专项敏感性：

- 次日额外高开成本 0%、1%、2%、3%、5%。
- 正式准入使用 2% 额外高开压力。
- 测试次日一字板无法成交。

### 5.2 观察策略

以下内容继续计算和报告，但初始正式权重为 0：

- 均线金叉。
- KDJ 超卖金叉。
- BOLL 均值回归。
- 连跌超跌反弹。
- 量价突破。
- 海龟突破。
- EOD 尾盘日线代理。
- 节假日效应。
- 网格交易。
- Alpha 对冲。
- 公司行为和指数调样事件。

观察策略只有在独立完成事件定义、样本外验证和准入后才能转为正式策略。

### 5.3 节假日策略

节假日效应样本少、频率低、容易被成本侵蚀，因此只能作为研究观察项。

低频策略不能因为样本少而降低数据真实性要求。若长期无法积累足够独立事件，应保持 `FAIL`，而不是调整口径凑样本。

### 5.4 网格交易

网格不是独立选股器，只能在仓位管理组已经批准的持仓内作为执行辅助。

建议：

```text
grid_center = rolling_mean_or_cost_basis
grid_step = ATR20 × multiplier
upper/lower = center ± k × ATR20
```

约束：

- 不得突破凯利目标仓位上限。
- 不得对负凯利股票补仓。
- 单次调仓金额必须覆盖交易成本。
- 趋势破坏、止损或政府冻结时停止网格。
- 回测必须单独归因网格带来的收益、成本和换手。

### 5.5 Alpha 对冲

第一版仅用于研究和风险归因，不默认接入真实卖空。

需要分别模拟：

- 股指期货对冲。
- ETF 代理对冲。
- 不对冲。

必须计入：

- 基差。
- 保证金。
- 展期成本。
- ETF 跟踪误差。
- 融券或做空可用性。

对冲不能掩盖股票选择本身无 Alpha。报告必须同时展示对冲前和对冲后收益。

---

## 6. StrategySignal 数据契约

统一结构至少包括：

```text
strategy_id
strategy_version
group_id
symbol
event_id
direction
raw_score
percentile_score
signal_timestamp
tradeable_timestamp
reference_date
signal_age
horizon_days
max_holding_days
historical_expected_return
historical_confidence
expected_cost
volatility_estimate
stop_loss
take_profit
source_columns
data_version
parameter_version
```

强制校验：

- `symbol` 必须包含市场前缀。
- `direction` 只能为 `long/short/flat`。
- `signal_timestamp <= tradeable_timestamp`。
- 未成熟事件不允许存在最终标签。
- 生成信号使用的所有数据时间不得晚于 `signal_timestamp`。
- 日线收盘信号的 `tradeable_timestamp` 至少是下一交易日。
- EOD 日线代理必须写入明确标记，不能伪装成分钟线策略。

---

## 7. 独立事件、标签与事件密度

### 7.1 独立事件

统计单位必须是独立事件，而不是重复的股票日。

规则：

- 信号首次触发创建 `event_id`。
- 信号持续期间只更新 `signal_age`。
- 同一事件未结束时的重复触发合并。
- 冷却期结束后才允许创建下一事件。
- 连续涨停形成一个事件簇。

### 7.2 事件密度预检

正式建模前，先对 2018–2024 年执行纯事件密度扫描，输出：

- 每个策略每年原始信号数。
- 独立事件数。
- 独立交易日期数。
- 每只股票事件分布。
- 行业事件分布。
- 每个持有期的成熟标签数。
- 冷却期前后事件数量变化。
- 事件收益自相关。
- 日期聚类后的有效样本量。

如果某策略年均独立事件不足 50 个，必须在进入模型开发前重新评估统计可行性。此阈值只用于预警，不代表自动准入。

### 7.3 多任务标签

每个成熟事件生成：

```text
classification_label = 成本后收益是否 > 0
regression_label = 成本后实际收益率
ranking_label = 同期横截面收益是否位于前20%
adverse_move = 持有期最大不利波动
favorable_move = 持有期最大有利波动
```

标签价格必须来自真实可交易价格，不得直接使用复权研究价格模拟成交。

### 7.4 标签消费者

- `classification_label`：胜率模型和概率校准。
- `regression_label`：期望收益和盈亏比。
- `ranking_label`：IC、NDCG、Top20% 命中率。
- `adverse_move`：策略风险分布和止损研究。
- `favorable_move`：止盈和持有期研究。

未来持有期不利波动只能用于历史统计，不能在买入决策时作为已知过滤条件。

---

## 8. 胜率、盈亏比与机器学习

### 8.1 贝叶斯胜率

使用 Beta-Binomial 收缩：

```text
alpha = wins + prior_strength × prior_p
beta = losses + prior_strength × (1-prior_p)
p_win_mean = alpha / (alpha + beta)
p_win_lower = Beta(alpha, beta) 的单侧下置信界
```

默认：

```text
prior_p=0.5
prior_strength=20
confidence_level=85%
```

敏感性测试：

- 先验强度 10、20、40。
- 下置信水平 80%、85%、90%。

正式凯利优先使用 `p_win_lower`，不使用未经收缩的滚动胜率。

### 8.2 盈亏比

默认固定使用 10% 截尾均值，避免动态改变截尾比例形成隐式调参：

```text
avg_win = 正收益的10%截尾均值
avg_loss = 负收益绝对值的10%截尾均值
payoff_ratio = clip(avg_win / avg_loss, 1.0, 3.0)
```

报告同时展示：

- 原始均值。
- 5% 截尾均值。
- 10% 截尾均值。
- 中位数。
- 尾部 95% 分位损失。

若 10% 截尾结果与原始均值差异超过 50%，标记“收益分布严重偏斜”，并降低可信度或禁止准入。

### 8.3 冷启动

独立事件样本不足时：

- 不允许使用未经收缩的高胜率。
- 不允许因为策略分数高而给予正常仓位。
- 默认目标仓位为 0。
- 如需模拟观察，可设置极低研究仓位，但不得进入正式组合绩效。

### 8.4 机器学习目标

第一版只使用简单、可解释模型：

- ElasticNet Logistic 预测盈利概率。
- Huber 或 ElasticNet 回归预测成本后收益。
- 排名模型只做监控。
- LightGBM 只作为挑战模型。

禁止直接使用 ML 输出控制最终仓位。

### 8.5 ML 融合

初始：

```text
p_final = 0.9 × p_bayesian + 0.1 × p_ml
```

ML 权重增加条件：

1. 连续两个、无中断的样本外窗口全部通过。
2. Brier Score 相对贝叶斯基线至少改善 10%。
3. ECE 不恶化。
4. AUC、NDCG 或 Top20% 命中率改善。
5. 成本后组合收益改善。
6. 最大回撤和换手没有显著恶化。

规则：

- 每次最多增加 0.05。
- 上限 0.30。
- 任一窗口失败，连续计数归零。
- 权重高于 0.10 时，失败立即回退到 0.10。
- 严重校准失败时 ML 权重直接归零。

### 8.6 概率校准

必须输出：

- Reliability Diagram。
- Brier Score。
- ECE。
- 分桶实际胜率。
- 概率分布。
- 各窗口校准漂移。

优先使用 Platt Scaling。只有样本充足、单调性稳定时才允许 Isotonic。

未经校准的技术分数只能参与候选排序，不能作为凯利概率。

---

## 9. 策略聚合、相关性与冲突

### 9.1 先组内后跨组

同一组内的 MACD、MA、EMA20 和海龟高度相关，不能当成四票独立证据。

流程：

1. 组内标准化。
2. 检测相关性和事件重合率。
3. 选择代表信号或进行受限聚合。
4. 每个组只输出一个组级证据。
5. 再对趋势、反转、量价、事件四组做跨组聚合。

### 9.2 证据函数

```text
evidence_i =
    direction_i
    × calibrated_confidence_i^gamma
    × expected_net_return_i
```

`gamma` 测试 1.0、1.5、2.0，默认 1.5。

### 9.3 冲突率

```text
conflict =
    min(long_evidence, short_evidence)
    / (long_evidence + short_evidence)
```

初始规则：

- `<0.30`：正常。
- `0.30–0.50`：降低目标仓位。
- `>0.50`：禁止新建仓。
- `>0.60` 且净收益转负：已有仓位减仓或退出。

策略组贡献上限初始为 35%。如果约束长期频繁触发，优先删除重复策略，而不是提高上限。

---

## 10. 凯利仓位管理

### 10.1 主公式

```text
p_loss = 1 - p_win_lower
kelly_raw = p_win_lower - p_loss / payoff_ratio
kelly_half = max(kelly_raw, 0) × 0.5
target_weight = min(kelly_half × market_discount, single_stock_cap)
```

必须用单元测试验证：

- `p=0.5, b=1` 时凯利为 0。
- 胜率提高时凯利单调提高。
- 盈亏比提高时凯利单调提高。
- 负凯利不能新建仓。
- 输入缺失、极端值和样本不足有确定结果。

### 10.2 约束

默认：

- 单票上限 5%。
- 单行业上限 20%。
- 单策略组上限 35%。
- 总仓位由政府层控制。
- 保留现金，不强制满仓。

停牌、涨跌停、容量和股票池状态是硬约束，不采用五六个折扣连续相乘。

### 10.3 持仓状态机

每个持仓必须处于以下状态之一：

```text
NEW
ACTIVE
HOLD_OUT_OF_POOL
REDUCE_LIGHT
REDUCE_MEDIUM
EXIT_PENDING
FROZEN_UNTRADEABLE
CLOSED
```

决策优先级从高到低：

1. 数据无效或交易冻结。
2. 停牌、跌停无法卖出等不可成交状态。
3. 组合级紧急去杠杆。
4. 止损、严重负凯利或预期收益显著为负。
5. 流动性持续恶化。
6. 中度凯利恶化。
7. 轻度凯利恶化。
8. 掉出股票池但凯利仍有效。
9. 正常持有或加仓。

### 10.4 掉出优质池

股票掉出优质池不等于立即卖出：

- 凯利有效、净预期收益为正：继续持有。
- 掉池后禁止主动加仓。
- 凯利轻度下降：减仓 25%。
- 凯利中度下降：减仓 50%。
- 连续 3 日低于持有阈值：逐步退出。
- 严重负凯利、止损或收益预测显著转负：全部退出。
- 无法成交：保留持久卖出意图，不能假设成交。

阈值必须由配置管理，并通过样本外敏感性分析确定，不允许根据单次回测临时调整。

### 10.5 加仓

只有同时满足以下条件才允许加仓：

- 当前目标权重大于实际权重和最小调仓阈值。
- 凯利为正。
- 股票仍在可买股票池。
- 无严重策略冲突。
- 容量允许。
- 政府层未冻结新开仓。
- 距上次普通调仓至少 3–5 个交易日。

趋势增强本身不能绕过凯利和容量约束。

---

## 11. 政府组合风险层

### 11.1 输出边界

政府层只输出：

```text
market_discount
portfolio_exposure_cap
trading_freeze_flag
emergency_deleveraging_flag
```

### 11.2 连续市场折扣

```text
raw_discount = sigmoid(
    a
    - b1 × volatility_percentile
    + b2 × market_breadth
    + b3 × index_trend
    - b4 × portfolio_drawdown
)

market_discount = clip(EMA5(raw_discount), 0.5, 1.0)
```

避免使用 0.6、0.8、1.0 三档离散跳变。

### 11.3 有限交互

允许：

```text
strategy_score × volatility_percentile
strategy_score × market_breadth
strategy_score × index_trend
```

要求：

- 交互项正则惩罚至少为主效应的 2 倍。
- 相邻窗口系数符号反转时冻结上一稳定值并报警。
- 连续 3 个窗口被冻结时，该交互项自动置 0。
- 自动退回无该交互项的简化模型。

### 11.4 优化目标

政府层不追求样本内收益最大，而是在约束下提高样本外净收益：

```text
最大化：成本后样本外净收益

约束：
最大回撤 <= 风险预算
换手率 <= 换手预算
容量约束全部满足
行业和单票上限满足
校准和数据门禁通过
```

---

## 12. 流动性、容量与交易成本

### 12.1 容量

默认成交参与率 2%，压力测试 1%、2%、5%、10%：

```text
capacity_i = ADV20_i × participation_rate
days_to_trade_i = order_value_i / capacity_i
```

必须报告：

- 每只股票预计成交天数。
- 组合最大可管理规模。
- 容量约束导致的未成交金额。
- 容量约束导致的现金比例。

### 12.2 流动性恶化

不使用组合中位数的相对阈值。采用股票自身历史分位数：

- ADV20 低于过去一年 5% 分位。
- 或 Amihud 高于过去一年 95% 分位。
- 且状态持续 3 个交易日。

处理：

- 单只股票触发：禁止加仓并降低目标仓位。
- 组合超过 20% 持仓同时触发：政府层进入流动性风险状态并批量降仓。
- 极端不可成交时保留订单，不伪造成交。

### 12.3 成本模型

至少包括：

- 买卖佣金。
- 卖出印花税，按历史日期使用对应税率。
- 过户费。
- 买卖价差。
- 滑点。
- 参与率冲击成本。
- 涨停后续的高开成本。
- 对冲的基差和展期成本。

交易成本参数必须按日期版本化。

---

## 13. 回测引擎验证

### 13.1 黄金测试

每一项必须有独立、可复现的小数据测试：

1. 信号在收盘生成，只能下一交易日成交。
2. 当日买入股票不能当日卖出。
3. 买入数量按 100 股整数手处理。
4. 涨停封死不能买入。
5. 跌停封死不能卖出。
6. 停牌期间仓位冻结。
7. 部分成交后的剩余订单继续存在。
8. 佣金最低收费正确。
9. 印花税只在卖出侧收取且历史税率正确。
10. 分红进入现金。
11. 送股改变持股数量和成本。
12. 复权研究价格不直接用于现金账本成交。
13. 指数调样只在生效日后进入股票池。
14. 交易失败不会修改成已成交仓位。
15. 现金、持仓市值和总资产守恒。

### 13.2 基准复现

主准入基准固定为沪深300全收益指数。

原因：

- 市场认知度高。
- 能够作为统一跨策略准入基准。
- 避免不同策略选择不同基准美化信息比率。

辅助报告同时展示：

- 沪深300价格指数。
- 中证500全收益指数。
- 中证A500全收益指数。
- 三指数等权组合。
- 股票池等权组合。

买入并持有沪深300成分或复制组合的回测必须在允许误差范围内接近官方全收益基准。误差需要拆解为：

- 成分更新误差。
- 分红误差。
- 费用误差。
- 调仓价误差。
- 数据缺失误差。

---

## 14. Walk-forward 与自动准入

### 14.1 切分

初始建议：

```text
训练窗口：5年
验证窗口：6个月
测试窗口：6个月
滚动步长：6个月
Purge：最长标签周期
Embargo：至少5个交易日
```

窗口边界必须按交易日计算。参数选择只能使用训练和验证窗口，测试窗口不可反复查看后调参。

### 14.2 参数稳定性

每个参数需要展示：

- 最优值。
- 邻近值绩效。
- 跨窗口选择频率。
- 绩效曲面。
- 小幅变化后的回撤和换手变化。

只有尖锐单点最优而邻近参数失效的策略，应判为过拟合。

### 14.3 频率分层

高频策略：

- 年独立事件 > 20。
- 至少 3 个样本外窗口。
- 每窗口至少 10 个独立事件。

低频策略：

- 年独立事件 < 10。
- 至少 2 个样本外窗口。
- 累计独立事件达到最低要求。
- 要求更高的信息比率和更强成本压力测试。

策略不能通过将重复股票日当成独立样本来满足门槛。

### 14.4 二元准入

只允许：

```text
PASS
FAIL
```

准入报告必须同时检查：

- `data_verified == True`。
- 执行引擎黄金测试全部通过。
- 点时股票池通过。
- 独立事件数量通过。
- 成本后收益通过。
- 相对沪深300全收益的信息比率通过。
- 最大回撤通过。
- 参数稳定性通过。
- 概率校准通过。
- 容量通过。
- 极端成本压力通过。
- 对现有组合有增量贡献。

任何一项失败，策略权重严格为 0。禁止人工覆盖。

---

## 15. 绩效评估与可视化

### 15.1 绩效指标

必须输出：

- 总收益。
- 年化收益。
- 年化波动率。
- Sharpe。
- Sortino。
- Calmar。
- 最大回撤。
- 最大回撤持续时间。
- 月度胜率。
- 交易胜率。
- 盈亏比。
- 最大连续亏损。
- 换手率。
- 交易成本占毛收益比例。
- Alpha、Beta。
- 信息比率。
- 相对主基准的超额收益。
- 容量使用率。
- 现金比例。
- 未成交比例。

### 15.2 归因

至少拆分：

- 选股贡献。
- 择时贡献。
- 凯利仓位贡献。
- 政府折扣贡献。
- 网格执行贡献。
- 行业暴露贡献。
- 对冲贡献。
- 现金拖累。
- 交易成本拖累。
- 不可成交拖累。

### 15.3 图表

必须生成：

- 资金曲线。
- 基准对比曲线。
- 回撤曲线。
- 月度收益热力图。
- 策略收益热力图。
- 股票池收益热力图。
- 因子相关性热力图。
- 策略信号冲突热力图。
- 凯利分布图。
- 胜率校准图。
- 行业暴露图。
- 容量使用图。
- 现金原因分布图。

每张热力图必须自动生成文字摘要，说明：

- 主要集中区域。
- 极端值。
- 结构变化。
- 是否存在单一时期或单一行业贡献过高。
- 是否触发风险警告。

未通过数据门禁时，所有图表必须加研究水印。

---

## 16. 配置与可复现性

### 16.1 单一配置入口

全局参数集中在统一配置系统中，按域拆分：

```text
data
universe
features
strategies
labels
calibration
ml
kelly
government
execution
costs
capacity
walk_forward
reporting
runtime
```

源码中不得散落业务阈值。

### 16.2 冻结项和可调项

配置项必须标注：

```text
FROZEN_STRUCTURE
TUNABLE_WITHIN_RANGE
RUNTIME_RISK_LIMIT
REPORT_ONLY
```

任何实验都保存：

- 完整配置快照。
- Git commit。
- 数据版本。
- 随机种子。
- 环境依赖。
- 运行开始和结束时间。
- 输入输出文件哈希。

---

## 17. 运行监控与自动降级

### 17.1 每日

- 实际仓位与目标仓位偏差。
- 现金比例和原因。
- 市场折扣和总仓位上限。
- 活跃信号数量。
- 策略组分布。
- 冲突率。
- 冻结资产。
- 失败和未完成订单。
- 容量使用率。
- 数据缺失和延迟。

### 17.2 每周

- 换手率。
- 成本占比。
- 各策略组权重。
- `p_win_mean`、`p_win_lower` 和 `payoff_ratio`。
- 概率校准漂移。
- 政府模型系数和符号。
- 流动性警报。
- ML 当前权重和连续通过计数。

### 17.3 每月

- 实际收益与历史预期偏差。
- 独立事件数量。
- 基准超额收益和信息比率。
- 策略 PASS/FAIL 状态。
- 容量与现金拖累。
- 数据降级次数。
- 模型回退次数。
- 实际成交成本与假设成本偏差。

### 17.4 自动降级

出现以下情况时自动降级：

- 数据版本校验失败：停止新开仓。
- 核心行情缺失：维持或减仓，不生成新信号。
- ML 校准失败：ML 权重回退。
- 政府模型不稳定：使用固定规则折扣。
- 容量大幅下降：降低目标仓位。
- 订单持续失败：保留退出意图并报警。
- 基准复现失效：所有正式准入冻结。

---

## 18. 模块建议

建议逐步形成以下模块边界：

```text
functions/data_sources/
functions/universe/
functions/features/
functions/strategies/
functions/events/
functions/labels/
functions/calibration/
functions/ml/
functions/aggregation/
functions/position_management/
functions/government/
functions/execution/
functions/backtest/
functions/admission/
functions/reporting/
functions/monitoring/
```

迁移原则：

- 不为目录整洁进行一次性重写。
- 新模块先提供兼容适配器。
- 每次只迁移一条可测试链路。
- 旧接口只有在所有调用方迁移并通过回归测试后才停止使用。
- 不删除历史结果文件，由新输出目录和版本号区分。

---

## 19. 分阶段实施

### Phase 0：数据完整性

交付：

- 数据白皮书。
- 字段级质量报告。
- 数据源适配接口。
- 历史指数成分覆盖报告。
- `data_verified` 门禁。
- 图表研究水印。

验收：

- 关键字段来源、时点和缺失处理可追溯。
- A500 缺失不会静默降级。
- 门禁不能被普通运行参数绕过。

### Phase 1：执行引擎

交付：

- 双价格视图。
- 公司行为账本。
- T+1、整数手、停牌和涨跌停。
- 成本模型。
- 持久订单和部分成交。
- 黄金测试。
- 沪深300全收益基准复现。

验收：

- 所有资产守恒测试通过。
- 基准误差在预先定义范围内。

### Phase 2：冻结四个策略

交付：

- 四个策略规则说明。
- 持有期。
- 冷却期。
- 参数搜索边界。
- 信号契约。

验收：

- 规则结构不可通过配置变成另一策略。
- 收盘信号不会同日成交。

### Phase 3：事件与标签

交付：

- 独立事件生成器。
- 2018–2024 事件密度报告。
- 多任务标签。
- 成熟度过滤日志。

验收：

- 重复股票日不会放大样本。
- `reference_date` 断言通过。

### Phase 4：统计与校准

交付：

- Beta-Binomial。
- 10% 截尾盈亏比。
- 概率校准报告。
- 冷启动规则。

验收：

- 小样本不会产生激进凯利。
- 概率可靠性可解释。

### Phase 5：冲突与 ML

交付：

- 组内去重。
- 跨组冲突。
- 线性 ML 挑战模型。
- ML 权重棘轮和回退。

验收：

- 高相关策略不会重复投票。
- ML 失败后立即回退。

### Phase 6：政府风险层

交付：

- 连续市场折扣。
- 总仓位上限。
- 系数稳定性监控。
- 三窗口冻结后置零。

验收：

- 政府层不能改写股票凯利概率。
- 模型失败可回退固定规则。

### Phase 7：凯利与持仓状态机

交付：

- 下置信界半凯利。
- 掉池继续持仓。
- 分级减仓。
- 加仓条件。
- 现金原因分类。

验收：

- 每项持仓动作有唯一、可审计原因。
- 规则冲突按固定优先级解决。

### Phase 8：Walk-forward 和准入

交付：

- Purge/Embargo。
- 参数稳定性。
- 二元自动准入。
- 容量和压力测试。

验收：

- 人工不能覆盖 FAIL。
- 测试窗口不参与调参。

### Phase 9：报告和模拟实盘

交付：

- 资金曲线和热力图。
- 完整绩效与归因。
- 日周月监控。
- 模拟实盘流水。

验收：

- 回测和模拟实盘使用相同信号、仓位和执行接口。
- 预测成本与实际模拟成本偏差可解释。

### Phase 10：扩展研究

在前九阶段全部通过后，再评估：

- LightGBM。
- 分钟线 EOD。
- 节假日。
- 网格增强。
- Alpha 对冲。
- 更多事件数据。

---

## 20. 测试矩阵

至少覆盖：

### 数据

- 缺失日期。
- 重复记录。
- 错误复权因子。
- 指数调样边界。
- 公告时间晚于交易时间。
- 数据源延迟。
- A500 历史缺失。

### 策略

- MACD 看多、RSI 看空。
- 同组多策略同时触发。
- 重复事件合并。
- 冷却期边界。
- 持有期边界。
- 空选股。

### 凯利

- 小样本高胜率。
- 低胜率高盈亏比。
- 胜率在 0.5 附近波动。
- 盈亏比极端异常。
- 凯利跨越持有阈值。
- 掉池但凯利仍正。

### 执行

- 连续涨停买不到。
- 连续跌停卖不出。
- 停牌跨调仓日。
- 部分成交。
- 流动性骤降。
- 成本超过毛收益。

### 政府层

- 市场波动率突然升高。
- 市场宽度恶化。
- 系数符号反转。
- 连续冻结三窗口。
- 组合回撤触发降仓。

### ML

- 单窗口改善但下一窗口失败。
- Brier 改善而 ECE 恶化。
- 权重从 0.15 回退 0.10。
- 输入特征缺失。
- 模型文件版本不一致。

---

## 21. 最终验收清单

正式版本只有同时满足以下条件才算完成：

- [ ] 所有参数集中配置并有版本。
- [ ] Phase 0/1 门禁通过。
- [ ] 三个指数池使用点时成分。
- [ ] 不合格股票有明确过滤原因。
- [ ] 指标、策略和消费者关系可追溯。
- [ ] 四个正式策略具备独立事件定义。
- [ ] 每个策略持有期和冷却期已冻结。
- [ ] 胜率采用贝叶斯收缩和下置信界。
- [ ] 盈亏比采用稳健统计。
- [ ] ML 权重受限且可自动回退。
- [ ] 策略冲突有明确行为。
- [ ] 凯利为单票主评分。
- [ ] 掉池持仓和分级退出已实现。
- [ ] 政府层只控制组合风险。
- [ ] 容量、现金和不可成交被真实记录。
- [ ] 回测包含完整成本和公司行为。
- [ ] 资金曲线、热力图、绩效和归因完整。
- [ ] 自动准入只有 PASS/FAIL。
- [ ] 所有正式结果均来自严格样本外测试。
- [ ] 模拟实盘稳定运行达到预设观察期。

---

## 22. 最终审视

该方案在工程上可落地，但不承诺所有策略最终都会通过。相反，一个严谨系统应允许多数策略因样本不足、成本过高、容量不足或样本外失效而被淘汰。

当前最重要的风险不是策略数量不够，而是：

1. 历史指数成分和公司行为仍未完全验证。
2. 事件密度可能低于建模需求。
3. 日频回测可能低估涨跌停和高开成本。
4. 高换手技术策略可能在完整成本后失效。
5. 多层模型可能传播概率估计误差。
6. 人工为了获得漂亮曲线绕过门禁。

因此实施顺序不能改变：

```text
先验证数据
→ 再验证执行
→ 再统计事件
→ 再校准概率
→ 再计算凯利
→ 最后讨论收益
```

第一项实际开发工作应是 Phase 0 数据完整性白皮书与 `data_verified` 强制门禁；第一项策略工作应是四个候选策略的事件密度扫描，而不是继续增加新策略或训练更复杂模型。
