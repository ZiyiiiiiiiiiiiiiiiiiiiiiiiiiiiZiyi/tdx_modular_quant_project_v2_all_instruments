# SCAP 控制冲突统一审计（2026-07-25）

## 1. 审计结论

25号历史运行暴露的主要问题不是“规则数量不够”，而是同一交易动作在生命周期、E阶段授权、组合策略、订单计划和待成交状态中被重复判断。此前部分判断没有共享唯一出口，造成登记了冷却却不执行、纸面高优先级原因遮蔽活动原因、卖出后旧买入条件立即恢复、Web显示与资金档案不一致。

本次把规则收敛为两类唯一出口：

1. `single_exit_authority_v2`：所有退出模块只提交触发证据；统一出口先逐项检查E阶段权限，再选择一个规范退出原因。
2. `sell_precedence_v1`：同一股票、同一决策只能保留一个方向；卖出优先于买入。新的卖出意图会取消同股票仍在等待的旧买单。

## 2. 已发现并修复的冲突

| 冲突 | 原行为 | 统一后的行为 | 状态 |
|---|---|---|---|
| 冷却登记与冷却权限冲突 | 卖出成交登记20日冷却，但SCAP把`cooldown`永久禁用，入场端不执行 | E1起冷却取得权限；小资金固定10个交易日；禁止高分绕过 | 已修复 |
| 信号失效与重新入场抖动 | 单日失效可卖出，次日重新满足入场即可买回 | 信号失效家族必须连续3个决策日确认，卖出后再执行10日冷却 | 已修复 |
| 多退出原因抢同一出口 | 多个模块同时触发，原因优先级和授权可能分散处理 | 所有触发先集中，逐项授权后只输出一个活动原因；同时保存触发、授权、否决和冲突数量 | 已修复 |
| 论文失效被并入普通信号失效 | 诊断无法区分“原假设坏了”还是“一般趋势坏了” | `thesis_failure_exit`保持独立原因，但继续使用E1信号失效权限 | 已修复 |
| 同股票同日买卖 | 策略行通常只有一个动作，但缺少最终防线 | 订单计划统一去重；同股票买卖并存时只保留卖出 | 已修复 |
| 旧买单与新卖单并存 | 前一日买单可能仍在pending，新退出又加入卖单 | 新卖出意图把旧买单标记为`superseded_by_sell_intent`；已有卖单时新买单直接过期 | 已修复 |
| Web与资金档替换开关不一致 | Web显示主动替换已勾选，但20,000元资金档实际关闭 | Web明确显示小资金主动替换关闭及重新开启条件 | 已修复 |
| 独立因子工具脱离主流程 | 必须人工抽取、另启服务、另做Excel | SCAP保存阶段自动生成CSV、Excel；monitor和结果页共同提供曲线与下载入口 | 已修复 |
| monitor独立脚本导入路径 | 产品接入后独立进程找不到项目包 | 增加包模式和直接脚本模式双路径导入 | 已修复并实测 |

## 3. 不是冲突、但必须明确的分层

### 3.1 纸面触发与活动触发

E1下E2/E3/E4信号仍会计算，但只能进入`paper_*`字段。统一仲裁会把未授权原因写入`exit_vetoed_reasons`。这不是互相打架，而是受控实验必须保留的反事实观察。

### 3.2 目标仓位与可执行仓位

目标仓位回答“信号希望持有多少”，可执行仓位回答“槽位、现金、一手和控制开关允许做到多少”。两者必须同时展示，不能强行改成同一个数。25号的巨大缺口仍然存在，但不能靠绕过交易约束消除。

### 3.3 主动替换继续关闭

满5只时确实存在大量更高分候选，但既有替换反事实净收益为负。统一控制不等于放开替换。重新开启必须满足固定样本外期限、完整双边成本后净收益均值及下置信界转正。

### 3.4 加仓继续关闭

本次没有启用亏损摊平或盈利加仓。投入资金收益仍为负，直接放开加仓可能放大亏损。盈利持仓分批加仓应作为独立实验，不与退出防抖同时归因。

## 4. 仍需下一次运行验证的影响

本次改变了未来E1的交易行为，因此25号历史结果不能作为修复后收益：

- 信号失效最早在连续第3次触发后才可卖出；
- 卖出后10个交易日不能重新入场；
- 同股票买卖冲突在订单计划和pending两个层级都会被拦截；
- 退出原因统计会更细，论文失效不再只显示为普通信号失效。

下一次必须使用相同数据窗、资本档案、因子柜、费用、PIT状态和代码指纹，分别运行旧E1对照与新E1防抖版本。至少对比净利润、最大回撤、卖出次数、10日内重复入场次数、平均持有期、费用、亏损尾部和相对基准。

## 5. 逐因子产品的正式链路

```text
SCAP回测保存
  -> 持仓账本 + Alpha提案 + 因子权重账本
  -> holding_factor_curves/
       -> holding_factor_scores_long.csv
       -> holding_daily.csv
       -> workbook_payload.json
       -> SCAP_持仓逐因子曲线.xlsx
  -> monitor /factors 与 /factor-workbook
  -> 结果页 /factors?key=... 与 /factor-workbook?key=...
```

因子曲线是事后只读诊断，不进入当日评分、换股或退出决策。

## 6. 发布判断

工程集成和冲突契约已通过测试，但策略仍不允许上线。原因是新规则尚未完成同口径受控回测，且25号旧E1的成本后期望、利润因子和相对基准均不合格。
# 2026-07-25 特殊版增量：四模块统一放权

用户随后明确授权主动换股、亏损摊平、盈利加仓和E2/E3/E4交易权。当前特殊版以E4累计授权退出，并把全部仓位动作接入`unified_position_action_v1`。统一交易出口不删除模块身份：订单及反事实奖励仍保存所有竞争提案、胜出模块、被否决模块和冲突数。

20日产品验证发现并修复两项新增冲突：

1. 首次入场漏斗错误把替换/加仓买腿计作新开仓，导致第11日假非单调并停止。现拆为首次入场、加仓和替换三种独立计数。
2. 替换选择器允许`position_state=blocked`挑战者，造成卖腿成交而买腿未登记。现要求挑战者仓位状态可执行；最终run的2组替换共4腿全部登记并成交。

最终有效短窗为`run20260725_215005`：20日总收益3.4532%、最大回撤-2.4374%、PF 1.2545、5笔闭合交易，但基准收益6.6737%、研究门禁仍阻断。亏损摊平和盈利加仓已开启但本窗口均未触发。该结果只证明产品链和冲突修复，不构成长期盈利或上线证据；完整经济判断必须使用模块消融和同口径长窗。
