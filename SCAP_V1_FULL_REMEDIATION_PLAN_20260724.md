# SCAP-V1 全链路完整修改方案

版本：2026-07-24
对象：小资金进攻盈利特殊版本（SCAP-V1）
主目标：成本后期末净利润
不可变硬约束：无杠杆、现金非负、持仓不超过配置上限、T+1、停牌/涨跌停、一手规则、PIT、账本守恒

## 1. 总体结论

当前问题不能归结为某一个止损参数。全链存在四类不同问题：

1. **状态机正确性问题**：E阶段权限、原因优先级、陈旧退出和硬止盈词典存在矛盾。
2. **决策口径问题**：同一天执行两次v3入场逻辑，生命周期和真实入场可能使用不同分数。
3. **容量与审计问题**：信号、现金、一手、槽位、优化器、订单未完全分层，下游漏斗仍使用旧语义。
4. **经济目标问题**：当前无量纲效用、一手固定组合、入场质量和准入门槛尚未与小资金“最终赚钱”完全一致。

正确顺序不是直接把E0改成E4，也不是立即提高仓位，而是：

`运行身份冻结 → 控制/原因状态机 → 单一评分链 → 六层容量漏斗 → 订单/账本 → 报告/Web → 准入重构 → 入场与利润优化 → E0-E4经济实验`

## 2. 完整薄弱点与修改合同

## 2.1 P0：控制注册表默认放行未知名称

### 当前问题

`scap_control_enabled()`对未识别的`control_name`默认返回`True`。任何拼写错误或新增控制若未登记，都可能绕过E0-E4阶段矩阵。

### 修改

- 建立唯一`SCAP_CONTROL_REGISTRY`，每个控制记录：
  - 规范名称；
  - 最低启用阶段；
  - 控制类型（entry/exit/risk/diagnostic）；
  - 是否允许SCAP使用。
- 未知名称一律抛出异常，不允许默认True。
- runner普通模式和SCAP模式分别有明确注册表，不共享模糊兜底。

### 验证

- 已登记控制逐项验证E0-E4布尔矩阵。
- 未登记、空字符串、大小写错误和近似拼写全部fail closed。
- Web/CLI预检与runner使用同一注册表。

## 2.2 P0：阶段授权顺序错误

### 当前问题

活动退出先从全部原始信号选择原因，再清除当前阶段未启用的原因。高阶段纸面信号可遮蔽低阶段已授权退出。

### 修改

分别构建：

```text
paper_signals  = 所有原始反事实信号
active_signals = 原始信号 AND 当前阶段权限
paper_reason   = priority(paper_signals)
active_reason  = priority(active_signals)
```

不允许对已选主原因做“清空后不回退”。

### 验证

对8类退出信号做全部布尔组合：

- 5个阶段 × 2^8组合；
- 验证未授权信号永不产生活动原因；
- 验证未授权高优先级信号不能遮蔽已授权低优先级信号；
- 验证纸面原因不受阶段影响。

## 2.3 P0：E1越级放行陈旧退出

### 当前问题

`stale_time_exit`在活动主原因清理时使用`signal_failure_exit`权限，因此E1可能放行本应E2才启用的陈旧退出。

### 修改

- `stale_time_exit`和`stale_time_reduce`只查询`stale_exit`。
- `signal_failure_exit`只管理信号/论点失败。
- 每个活动主原因必须与对应活动布尔列一致：

```text
position_exit_reason == X  =>  X_active_flag == True
exit_state == bool(position_exit_reason)
```

### 验证

- E1：signal可活动、stale必须纸面。
- E2：signal/post-entry/stale可活动。
- 每日状态账本执行原因—布尔列一致性断言。

## 2.4 P0：退出原因词典不统一

### 当前问题

生命周期使用`profit_hard_stop_exit`，订单优先级和执行模块使用`hard_stop_exit`，导致硬止盈退化为`normal_sell`。

### 修改

建立`ExitReason`唯一词典和兼容映射。建议规范名称：

- `profit_hard_stop_exit`
- `profit_giveback_exit`
- `loss_containment_exit`
- `post_entry_failure_exit`
- `signal_failure_exit`
- `thesis_failure_exit`
- `stale_time_exit`
- `stale_time_reduce`
- `alpha_collapse_consensus`
- `safety_deleveraging`
- `replacement_opportunity_exit`

历史`hard_stop_exit`只作为读取兼容别名，写出时统一为`profit_hard_stop_exit`。

订单增加独立字段：

- `liquidation_intent`：full/partial/none；
- `origin_exit_reason`；
- `current_exit_reason`；
- `reason_schema_version`。

执行是否全平由`liquidation_intent`和目标数量决定，不再依赖散落的原因字符串集合。

### 验证

每个规范原因完成：

`生命周期 → 订单 → pending → 成交 → trade pair → PnL by reason → control summary → Web`

逐字段完全一致。

## 2.5 P0：同一天执行两次v3入场逻辑

### 当前问题

runner先用原始cabinet分数执行一次`apply_mainline_v3_entry_policy()`供生命周期使用，之后再做风险惩罚/ML融合并执行第二次。结果：

- 生命周期信号失败使用第一次分数；
- 真实入场使用第二次最终分数；
- 月度ML版本的退出并非基于ML最终信号；
- `pre_v3_entry_confirmed`实际可能表示“第一次v3选择”，名称失真。

### 修改

把当前函数拆为三个纯阶段：

1. `attach_v3_compatibility_fields()`：只补字段，不选择、不改状态。
2. `build_final_daily_ranking()`：完成规则、ML融合、风险惩罚和覆盖状态。
3. `select_v3_entries_once()`：生命周期约束完成后只选择一次。

推荐日内顺序：

```text
原始候选
→ 最终评分/ML/风险分数
→ 持仓生命周期用同一最终分数评估
→ E阶段授权退出
→ 新入场优化器只运行一次
→ 订单
```

### 验证

- 每个候选记录`lifecycle_score_value/source/version`和`entry_decision_score_value/source/version`。
- 除明确的持有期限价值模型外，两者的当前信号基准必须一致。
- 每日优化器调用次数断言为1。
- v3、v3_ml和v31分别回归。

## 2.6 P1：信号—资本—槽位只完成部分拆分

### 当前问题

已拆开“槽位前信号”和“优化器选中”，但槽位前信号仍包含当前现金和一手可行性。现金不足仍会反向把策略信号数变成0。

### 修改

建立六层互斥字段：

1. `raw_positive_signal_count`
2. `structural_lot_feasible_count`
3. `cash_feasible_count`
4. `slot_feasible_count`
5. `optimizer_selected_count`
6. `retail_executable_order_count`

仓位公式：

```text
desired_exposure = f(raw_signal_strength, risk_ceiling)
executable_exposure = min(
    desired_exposure,
    actual_exposure + executable_increment
)
```

现金、一手和槽位只能影响`executable_exposure`，不得回写`desired_exposure`。

### 验证

构造四类反例：

- 有信号但现金不足；
- 有信号但槽位为0；
- 有现金但无正信号；
- 有正信号但一手穿透40%上限。

每层计数和拖累归因必须唯一。

## 2.7 P1：信号数量严重饱和

### 当前问题

正效用信号每天121至136个，但仓位函数4个以上就达到最高档，且效用为当日横截面分位数，弱正值与强信号无法区分。

### 修改

不再仅用数量。预登记三种候选指标后做A/B：

- Top-5效用均值/最小值；
- Top-K成本后绝对预期收益LCB；
- 强信号数：超过固定绝对门槛而非仅`>0`。

在预期收益尚未校准前，使用Top-5效用强度作为诊断，不立即增加实际仓位。

### 验证

- 横截面整体平移/缩放后行为明确；
- 大量接近0的弱信号不能等价于少量强信号；
- 指标按日期可比较或明确标记“仅当日横截面”。

## 2.8 P1：候选漏斗混合不同母集

### 当前问题

五日产品有4/5天后层`capital_pass_count`大于前层`entry_confirmation_pass_count`。新候选、已有持仓、理想组合和跨日订单混在同一漏斗。

### 修改

拆为三张表：

1. `new_entry_funnel_daily`：同一新候选母集，严格单调不增。
2. `holding_maintenance_daily`：已有持仓的保留、退出、加仓状态。
3. `order_transition_daily`：决策日订单、待执行、成交、阻塞、过期。

新入场漏斗必须满足：

```text
raw ≥ score ≥ structural ≥ cash ≥ slot ≥ optimizer ≥ order ≥ fill
```

### 验证

- 每日自动检查单调性。
- 不同统计范围禁止计算通过率。
- 决策日和成交日通过`decision_id/order_id`连接，不在同一列伪装为同步。

## 2.9 P1：追仓诊断可能与容量事实不一致

### 当前问题

`catchup_allowed`使用策略信号数和目标仓位，但不直接知道剩余槽位/可执行增量。虽然当前policy通常不会凭空买未选中候选，报告仍可能显示“允许追仓”但实际无容量。

### 修改

分别输出：

- `catchup_signal_authorized`
- `catchup_capacity_available`
- `catchup_order_created`

只有三者同时为真才显示“可执行追仓”。否则给出唯一阻塞原因。

### 验证

满槽位、无现金、一手不可行、风险门禁和无信号五种场景。

## 2.10 P1：控制触发摘要不完整

### 当前问题

摘要遗漏损失控制、陈旧退出和论点失败，不能完成纸面→活动→订单→成交四段对账。

### 修改

摘要从`ExitReason`注册表自动生成，不手写子集。每个原因输出：

- evaluated
- paper_trigger
- active_trigger
- order_created
- pending
- filled
- blocked
- expired
- closed_trade

### 验证

对每一原因断言：

```text
filled ≤ order_created ≤ active_trigger
closed_trade ≤ filled
```

允许跨日时按`decision_id/order_id`累计，而不是按同一天硬比较。

## 2.11 P1：运行身份不足以支持E阶段比较

### 当前问题

输出目录没有E阶段，策略摘要和准入报告没有`scap_exit_stage/scap_loss_stop`，环境manifest也未显式记录CLI覆盖、代码状态和原因schema。不同E阶段很容易被误比较。

### 修改

运行身份必须包含：

- SCAP版本；
- E阶段；
- loss stop；
- 资金档位、初始现金、缓冲、持仓上限；
- 股票池、因子柜hash；
- 日期窗；
- 成本模型；
- PIT状态；
- 代码commit或工作树文件hash；
- 控制/原因/评分schema版本；
- 完整有效CLI参数。

输出路径建议：

`.../ctrl_aggressive_profit/exit_E1/loss_12pct/v3/run...`

摘要、准入、Web和比较器全部显示该身份。

### 验证

- 任一身份字段不同，比较器拒绝标记为受控单变量实验。
- manifest缺字段时run只能标记`incomparable_research_run`。

## 2.12 P1：pending订单原因只保留单一语义

### 当前问题

同一股票已有卖单时，只有新原因优先级严格更高才更新；同优先级原因演变会丢失。第一原因和当前原因混为一列。

### 修改

保留：

- `origin_reason`
- `latest_reason`
- `highest_priority_reason`
- `reason_history`

成交归因同时报告起因和最终执行原因。全平仓意图独立持久化。

### 验证

模拟信号失败→损失控制→涨跌停阻塞→最终成交，检查原因历史和成交归因。

## 2.13 P1：公司行动进入账户但不进入交易PF

### 当前问题

账户NAV包含分红等公司行动现金，但`trade_pairing.py`只读取买卖成交。终端净利润与交易PF不是同一经济口径。长持有/分红策略会被PF低估。

### 修改

交易对新增：

- holding-period cash dividend；
- dividend tax；
- stock dividend/split basis adjustment；
- `total_return_realized_pnl`；
- `price_only_realized_pnl`。

PF主口径使用成本、税和公司行动后的总回报；同时保留价格交易PF作诊断。

### 验证

现金分红、送股、拆并股、除权前后卖出、期末未平仓五类黄金账本。

## 2.14 P1：准入门槛与五槽位右尾风格冲突

### 当前问题

准入要求至少504日和150笔闭合交易。最多5只、20日主期限下，理论满负荷约只能产生：

`5 × 504 / 20 ≈ 126笔`

150笔门槛隐含平均持有低于16.8日，会逼策略高换手，与右尾持有目标冲突。

两年窗口下`positive_year_slice_share ≥ 0.60`实际要求2/2年份全部盈利，存在离散门槛突变。

### 修改

用“证据量”而非固定150笔：

- 最低独立闭合样本；
- 有效持仓机会数；
- bootstrap置信区间；
- 交易日/市场状态覆盖；
- 滚动12个月切片，而非只有2个年度点。

主门槛保持：

- 终端成本后净利润为正；
- PF兜底；
- 胜率或盈亏比二选一；
- 成本压力；
- 无账本/PIT失败。

建议先以40–60笔闭合交易作为研究阶段下限，再由功效分析确定正式值；不是直接降低生产标准。

### 验证

构造低频右尾、高频小利和无交易三类策略，确保门槛不偏袒高换手，同时拒绝证据不足。

## 2.15 P1：候选效用不是净利润目标

### 当前问题

效用主要由分位数和相对惩罚组成；成本惩罚也是横截面排名。即使全市场成本都很高，也会有“相对低成本”候选。现金碎片惩罚允许留下大量现金不受罚。

### 修改

分两步：

1. 先建立样本外`expected_net_return`和LCB，单位必须是收益率。
2. 再求整数手数利润：

```text
maximize Σ LCB(expected_net_return_i,h) × notional_i × lots_i
         - overlap_penalty_amount
         - concentration_penalty_amount
```

不允许把分位数直接当收益率。

### 验证

- 相同效用不同金额；
- 相同金额不同净收益；
- 高固定佣金小订单；
- 空组合优于负期望组合；
- 新旧优化器同窗口A/B。

## 2.16 P1：入场短期质量不足

### 当前问题

5/10日表现为负，20日绝对收益为正但超额为负。样本只有5笔，不足以重拟合。

### 修改

- 5日只作为灾难风险约束；
- 20日成本后绝对收益作为主目标；
- 市场中性超额作为Alpha诊断，不替代赚钱目标；
- 逐项消融质量软惩罚，不同时改变多个门槛；
- 所有阈值滚动样本外确定。

### 验证

报告5/10/20日均值、LCB、命中率、盈亏比、尾部贡献、成本压力和年度/状态切片。

## 2.17 P2：Web仍使用旧字段与不完整词典

### 当前问题

Web只显示`entry_confirmed_count`，不显示大量槽位前信号；损失控制原因缺中文映射；E阶段身份不够醒目。

### 修改

Web显示：

- 原始信号、结构可行、现金可行、槽位、优化器、订单、成交；
- 当前E阶段及每类控制实际启用状态；
- 纸面原因与活动原因并列；
- 规范原因中文词典；
- 运行身份和可比性状态。

### 验证

API字段、页面文本和CSV三方一致；0选中但有信号时不得显示“无信号”。

## 2.18 P2：正式回测默认携带大量非决策影子组合

### 当前问题

74个影子组合显著放慢长窗，但不参与SCAP当前交易控制。

### 修改

- `scap_formal_fast`：关闭非决策影子，仅运行正式组合。
- `scap_full_diagnostics`：独立运行完整影子归因。
- 两者先验证正式交易、订单、NAV逐日一致。

### 验证

逐日hash比较正式组合产物；不一致则快速模式不得使用。

## 3. 分阶段施工计划

## Phase A：运行身份与冻结基线

修改范围：

- manifest、输出目录、摘要和准入身份字段；
- 不改变任何交易。

验收：

- E0五日结果与当前基线逐日NAV/订单一致；
- 缺身份字段时比较器拒绝比较。

## Phase B：控制注册表与退出状态机

修改范围：

- fail-closed控制注册表；
- 先授权后优先级；
- 陈旧退出阶段；
- 原因规范词典；
- `liquidation_intent`。

禁止同时修改：

- 入场、仓位、止损阈值、股票池、成本。

验收：

- 5×2^8组合状态测试；
- 每个原因一条订单—成交闭环黄金测试；
- E0短链交易结果保持不变。

## Phase C：单一最终评分链

修改范围：

- 拆分兼容字段、最终评分和一次选择；
- 生命周期绑定最终评分来源。

禁止同时修改：

- 因子权重和ML权重。

验收：

- 每日优化器只调用一次；
- v3/v3_ml/v31生命周期与入场分数来源可对账；
- 当前E0配对短链结果差异必须被解释，不能静默变化。

## Phase D：六层候选—资本—槽位漏斗

修改范围：

- 六层字段；
- desired/executable公式；
- catchup三层状态；
- 单调新入场漏斗。

验收：

- 现金不足、满槽位、一手超限、无信号合成场景；
- 五日产品所有漏斗日单调；
- Web和CSV一致。

## Phase E：订单、pending、账本和公司行动

修改范围：

- 全平仓意图；
- 原因历史；
- 公司行动进入交易总回报；
- 控制摘要自动生成。

验收：

- T+1、涨跌停、停牌、部分成交、奇数股全平；
- 分红/送股黄金账本；
- 账户PnL与已实现+未实现+公司行动对账误差为0。

## Phase F：报告、Web与准入重构

修改范围：

- 新字段和规范词典；
- 准入证据量；
- 滚动切片；
- 可比性门禁。

验收：

- 报告、API、Web、CSV一致；
- 右尾低频、高频和无交易策略的准入方向符合合同。

## Phase G：入场与利润优化研究

修改范围：

- 入场消融；
- 预期净收益校准；
- 利润金额整数优化器。

禁止同时修改：

- E阶段、加仓、替换、股票池。

验收：

- 走样本外20日成本后期望/LCB；
- 新旧优化器单变量A/B；
- 成本压力和资本利用解释。

## Phase H：E0-E4退出锦标赛

前置条件：Phase A–F全部通过，Phase G至少证明基础入场成本后期望不是负值。

固定：

- 日期、代码hash、数据、PIT、股票池、资金、成本、因子柜、入场、优化器、加仓和替换。

唯一变量：

- `scap_exit_stage`。

比较：

- 终端成本后净利润；
- PF；
- 已实现/未实现PnL；
- 右尾贡献；
- 平均持有期和换手成本；
- 最大回撤仅作为风险披露，不代替利润；
- 年度/滚动12月/市场状态切片。

## 4. 测试与产品验收矩阵

| 层级 | 必须通过 |
|---|---|
| 语法 | 修改Python全部`py_compile` |
| 纯逻辑 | 控制矩阵、组合触发、原因规范、六层计数、准入数学 |
| 模块 | lifecycle、policy、pending、execution、trade pairing、report、Web |
| 集成 | v3、v3_ml、v31；E0-E4；T+1/停牌/涨跌停 |
| 产品短链 | 5日，真实数据，CSV/API/Web字段对账，stderr=0 |
| 中链 | 至少覆盖一次退出、一次阻塞卖单和一次公司行动 |
| 经济长链 | 同身份配对，成本压力、切片和准入 |

每阶段必须产出：

- 修改前失败用例；
- 修改后通过用例；
- 受影响主线回归；
- 五日产品结果；
- WBS变更记录；
- 是否改变交易行为；
- 是否允许进入下一阶段。

## 5. 明确不同时做的事情

- 不同时修退出和入场。
- 不同时启用加仓与替换。
- 不同时改变持仓数和E阶段。
- 不用短链证明盈利。
- 不用更高仓位掩盖负期望。
- 不因用户能承受回撤取消无杠杆、现金、T+1和结构上限。
- 不把胜率和盈亏比同时设为高硬门槛；PF兜底后只要求其中一条盈利路径。

## 6. 推荐最终产品形态

短期不是“全部控制都开”的版本，而是三个清晰产品：

1. **SCAP-E0诊断基线**：所有退出纸面化，只用于理解路径。
2. **SCAP研究冠军**：E1-E4受控比较后的最佳阶段，只能研究使用。
3. **SCAP前瞻纸面版**：锁定参数、无回看修改，完成足够前瞻证据后才讨论真实资金。

## 7. 2026-07-24实施与20日验收结论

Phase A-H 已全部实现，并在每阶段执行专项测试、受影响回归和真实5日产品短链。最终20日E0全流程运行目录为：

`results/decision_council/scap/cab_c6dae8d4d69c/e0_l1200/v3/run20260724_223951`

验收事实：

- 20个交易日全部完成，stderr为0，125个文件落盘；
- 109个CSV均可由普通Windows PowerShell `Import-Csv` 读取；
- 最终资金20,402.200968元，成本后净利润402.200968元，总收益2.0110%，最大回撤-3.8840%；
- 同期研究基准收益6.6737%，超额收益-5.1270%；
- 5笔买入、0笔卖出、0笔闭合交易，因此胜率、盈亏比和利润因子均没有闭合交易证据；
- E0下15次买后失败只作为纸面触发，活动触发、订单和成交均为0；
- 六层漏斗20日全部满足单调不增，运行完整性10项全部通过；
- 20日人民币利润审计中，优化器选中样本9条，平均47.91元；未选中样本760条，平均173.99元。当前排序尚未证明能最大化最终人民币利润。

当前状态：Phase A-H和20日工程验收完成；仍为`research_gate_blocked`，不得上线。下一轮只能在相同数据、代码、成本、股票池、资本档案、因子柜和评分身份下，按E0→E1→E2→E3→E4逐级做单变量比较。优先研究滚动样本外`E[net_profit_yuan_20d]`入场排序；在完成预注册校准前，该指标保持事后审计，严禁进入当日决策以避免未来数据泄漏。
