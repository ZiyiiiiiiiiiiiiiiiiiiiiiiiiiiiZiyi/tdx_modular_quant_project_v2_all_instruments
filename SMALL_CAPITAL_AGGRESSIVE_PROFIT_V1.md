# 小资金进攻盈利特殊版方案（SCAP-V1）

版本：设计稿 v1.0
日期：2026-07-23
策略标识：`small_capital_aggressive_profit_v1`
当前状态：`research_candidate`，无实盘交易权
适用账户：初始资金约 20,000 元、无杠杆、可接受较大净值波动、以成本后最终净利润为首要目标

## 1. 执行摘要

SCAP-V1 不是把现有治理系统的限制全部关闭，也不是简单提高仓位。它是一条与 `factor_only`、`safe_factor_only` 和 `normal` 隔离的“小资金利润主线”：

1. 用规则因子柜或经隔离验证的月度 ML 产生横截面排序。
2. 将流动性、波动、短期跌幅等未经证明的硬门槛改为可消融软惩罚，只保留不可违反的生产硬约束。
3. 在少量高质量候选中，按一手金额、交易成本、集中度和剩余现金碎片进行组合级选择。
4. 默认最多持有 5 只，研究 3/4/5 只，允许现金，不强制满仓。
5. 默认禁止向亏损持仓摊低成本；先验证“截断失败交易、保留右尾赢家”的简单退出族。
6. 主动替换继续关闭，只有样本外、成本后反事实收益通过准入才可重新申请交易权。
7. 首先看成本后最终净利润和 Profit Factor；胜率与盈亏比采用二选一路径，不要求同时很高。

该版本利用的真实优势：

- 一手和最多五只持仓使搜索空间较小，可以做接近精确的资金分配。
- 20,000 元账户容量压力低，可研究大资金不愿进入但仍真实可交易的中小盘标的。
- 可以允许现金等待，不需要为跟踪基准而被迫买入。
- 可以集中持有少数高质量机会，让少数大盈利交易贡献主要利润。
- 可以承受较大波动，因此无需为了净值平滑而频繁换股、过早止盈。

## 2. 目标函数与底线

### 2.1 首要目标

正式比较策略的第一排序键：

`TerminalNetProfit = FinalNAV_after_all_costs - InitialCash`

必须逐笔扣除佣金、最低佣金、印花税、过户费、滑点和冲击成本。

为防止只优化一个偶然幸运的终点，研究排序另用：

`RobustProfitScore = Median(NetProfit_slice) - λd×DownsideInstability - λc×CostFragility`

`RobustProfitScore` 不能替代真实净利润。各惩罚定义必须实验前登记，不得看结果后修改。

### 2.2 胜率与盈亏比

`Expectancy = WinRate×AvgWin - (1-WinRate)×AvgLoss - PerTradeCost`

统一底线：

- 成本后总净利润为正。
- 成本后 Profit Factor 大于 1，并达到预登记稳健门槛。
- 在此前提下允许二选一：
  - 胜率路径：较高胜率，盈亏比不设独立高门槛。
  - 右尾路径：较高盈亏比，胜率不设独立高门槛。

SCAP-V1 优先研究右尾路径，因为小资金、低换手、集中持仓和允许较大波动更适合“少数赢家贡献大部分利润”。这是待验证假设，不是预定结论。

### 2.3 不可交换的生产硬约束

- 无杠杆、现金非负。
- 库存不下穿，不卖不可用股份。
- 遵守一手、T+1、停牌、涨跌停和板块交易单位。
- 决策日与执行日严格分离，不使用执行日收盘数据成交。
- PIT 成分、PIT 基本面、标签成熟日和训练隔离必须通过。
- 订单、成交、现金、持仓和 NAV 逐日守恒。
- 数据、模型 schema 或账本异常时 fail closed。

### 2.4 大回撤偏好

大回撤容忍只降低对净值平滑的偏好，不给负期望、错误模型或无边界摊亏授权。

暂定研究级灾难边界：

- 账户回撤 35%：警报并停止提高风险预算。
- 账户回撤 45%：停止新开仓，人工复核。

这两个数只是研究默认值，不代表用户已授权真实资金承受 45% 损失；实盘前必须确认可损失金额。

## 3. 版本隔离与复用

### 3.1 独立身份

- 策略版本：`small_capital_aggressive_profit_v1`
- 控制模式：`aggressive_profit`
- 资金档位：`small_capital_branch`
- 输出片段：`ctrl_aggressive_profit/scap_v1`

不得把该版本做成 `factor_only` 的改名参数。`factor_only` 是因子诊断模式，关闭多个退出控制，不具备完整利润策略语义。

### 3.2 直接复用

- PIT 数据、历史指数成分和交易日历。
- 因子注册、家族聚合、角色评分和因子柜。
- 月度 LGBM 的时间隔离、训练和连续排序融合。
- 一手、T+1、涨跌停、费用、冲击成本和执行账本。
- 现金、持仓、交易配对、NAV 和报告基础设施。
- 候选漏斗、反事实 reward、失败实验室和研究门禁。

只新增或扩展：

- 小资金候选效用。
- 最多五只持仓的一手组合优化。
- 三层仓位目标。
- 简化退出族及独立消融。
- 小资金自适应研究门禁。
- 盈利指标唯一语义合同。

## 4. 全链路设计

### 4.1 股票池

阶段 A 继续使用当前 `hs300 + csi500 + a500` PIT 严格池，建立可比较基线。

阶段 B 才研究扩大股票池：

- 排除 ST、退市整理、停牌和上市时间不足标的。
- 使用历史时点可知的证券状态和成员信息。
- 设置绝对成交额底线和账户参与率上限。
- 保留涨跌停、一手可买和公司行动处理。
- 不把“小资金”直接等同于微盘股或低价股。

阶段 B 必须与 A 同期、同成本、同信号、同退出比较，只有成本后利润和稳健性更好才获得交易权。

### 4.2 Alpha 主线

1. `v3` 规则因子柜作为基线。
2. 修复后的 `v3_ml` 作为挑战者。
3. ML 只能连续调整排序，不能绕过生产硬约束。
4. ML 验证 IC 或稳定性不合格时，权重收缩至规则分数。

同一实验不得同时更换因子柜、ML、股票池、退出和仓位。

### 4.3 候选漏斗

生产硬过滤只包括：

- PIT 股票池合法。
- 当日状态允许形成信号。
- 下一执行日规则上可能成交。
- 价格、成交量和必要特征非异常。
- 一手金额不突破账户结构硬上限。
- 泄漏与 schema 审计通过。

下列现有条件先作为实验性软惩罚，不默认全部 AND：

- 波动不高于中位数 1.5 倍。
- 当日和 20 日均成交额高于最低额 2 倍。
- 20 日收益不低于 -5%。
- 5 日收益不低于 -3%。
- 总分位必须处于前 20%。

任何软条件只有在多个隔离窗口提高成本后净利润或 PF，才可升级为硬门槛。

### 4.4 小资金候选效用

`Utility_i = AlphaPct_i - λcost×CostPct_i - λconc×ConcentrationPenalty_i - λfrag×CashFragmentPenalty_i - λcorr×OverlapPenalty_i`

- `AlphaPct`：规则或 ML 融合后的横截面分位数。
- `CostPct`：完整往返成本分位数，包含最低佣金敏感性。
- `ConcentrationPenalty`：一手市值超过单仓软上限后的惩罚。
- `CashFragmentPenalty`：买入后形成难以使用的剩余现金惩罚。
- `OverlapPenalty`：与现有持仓行业、因子暴露或收益相关性的惩罚。

各分量先标准化。λ 只能是预登记实验参数，不得在最终测试窗口调优。

### 4.5 一手组合优化

在每日前 K 个候选上搜索有限组合，不再只按分数逐只贪心。

`Max Σ Utility_i×Weight_i - PortfolioConcentrationPenalty - ExpectedRoundTripCost`

约束：

- 持仓数不超过 5。
- 每只为交易单位整数倍。
- 现金不低于缓冲。
- 单股结构硬上限 40%。
- 不违反已有持仓、T+1 和待成交订单。
- 不强制最低持仓数。

比较最大 3、4、5 只，以成本后净利润、PF 和跨窗口稳定性选择，不预设越集中越好。

### 4.6 三层仓位

必须分列：

1. `risk_exposure_ceiling`：安全和账户允许的最高仓位。
2. `desired_exposure_target`：信号数量和强度对应的期望仓位。
3. `executable_exposure_target`：一手、现金、涨跌停和候选限制后的可实现仓位。

不得用第三项覆盖第二项。差异需拆解为无信号、一手不可买、现金碎片、订单阻塞和风险上限拖累。

初始研究映射：

| 高质量可买候选数 | 期望仓位研究区间 |
|---:|---:|
| 0 | 不强制新增 |
| 1 | 20%–40% |
| 2 | 40%–70% |
| 3 | 60%–90% |
| 4+ | 75%–95% |

最终仓位不得超过安全上限和可执行上限。

### 4.7 入场

- 先过生产硬约束，再按候选效用做组合选择。
- 新标的从一手开始，除非组合优化明确允许更多手。
- 相似因子不得重复投票。
- 买入理由记录 Alpha、成本、集中度、现金碎片和组合边际贡献。
- 每个门槛标注 `trading`、`paper`、`diagnostic` 或 `disabled`，禁止配置显示开启但实际被 v3 覆盖。

### 4.8 持有与加仓

第一阶段：

- 禁止向浮亏持仓加仓。
- 禁止因摊低成本扩大风险。
- 现有亏损分层加仓只保留诊断，不获得交易权。

第二阶段才单独研究赢家加仓：

- 已有成本后浮盈。
- 原始 Alpha 未衰减。
- 趋势或确认信号增强。
- 加仓后仍满足单仓和组合限制。

必须与“不加仓”独立 A/B，不与退出改造同时上线。

### 4.9 退出

不启用 `normal` 的全部复杂控制，也不使用缺少失败退出的裸 `factor_only`。建立互斥、可消融的简单退出族：

- E0：只有生产安全退出，作为基线。
- E1：Alpha/排名显著衰减退出。
- E2：时间失败退出——持有一定交易日仍未产生足够 MFE。
- E3：损失控制退出——测试无止损、-8%、-12%，不得看完最终窗口再挑。
- E4：右尾利润保护——达到明显浮盈后才启动跟踪保护。

顺序为 E0→E1→E2→E3→E4，每次只加一个组件。评价净利润、PF、平均赢亏、避免损失、错杀赢家机会成本、换手和成本增量。

主动替换不属于基础退出族，继续关闭。

### 4.10 执行与成本

若最低佣金未知，至少报告：

- 0 元最低佣金。
- 1 元最低佣金。
- 5 元最低佣金。

压力测试：

- 基准成本。
- 滑点和冲击成本 1.5 倍。
- 滑点和冲击成本 2 倍。
- 次日无法成交或延迟成交。

利润必须在基准成本下为正，压力下不能结构性崩溃。单独报告成本占初始资本、成本占毛利润和每笔成本。

### 4.11 安全边界

保留：

- 数据、价格、交易日历或 PIT 完整性失败冻结。
- 现金、库存、NAV 或订单账本不守恒冻结。
- 涨跌停、停牌、T+1 不可成交。
- 单股或账户灾难回撤冻结。
- 成本、换手或滑点超出压力边界冻结。

安全模块只限制风险，不参与 Alpha 奖励。

## 5. 指标唯一语义

### 5.1 指标层级

| 层级 | 指标 | 用途 |
|---|---|---|
| P1 | `terminal_net_profit_after_cost` | 第一目标 |
| P1 | `profit_factor_after_cost` | 正期望兜底 |
| P2 | `net_expectancy_per_closed_trade` | 单笔经济性 |
| P2 | `payoff_ratio`、`win_rate` | 判断盈利风格，二选一 |
| P2 | `cost_to_gross_profit` | 成本脆弱性 |
| P3 | `max_drawdown`、恢复时间 | 风险披露和灾难线 |
| P3 | 基准超额收益 | 机会成本，不作首要硬门槛 |

### 5.2 前瞻收益命名

禁止无口径后缀的 `buy_expectancy_10d`，统一为：

- `buy_forward_return_10d_gross`
- `buy_forward_return_10d_after_cost`
- `buy_forward_excess_return_10d_after_cost`
- `buy_directional_proxy_reward_10d`

准入引用哪个字段必须唯一配置并写入快照。

### 5.3 小资金集中度

研究报告同时展示：

- 结构硬上限。
- 一手可行条件下最优可达集中度。
- 实际与可达集中度之差。
- 机构式 25% 门槛，仅作对照。

不能仅因一手离散导致单仓 31% 就按机构标准否决；也不能因资金小而忽略 40% 单仓的永久损失风险。

## 6. 实验设计

### 6.1 已污染窗口

2025-01 至 2026-05 已被多次检查并用于修改逻辑，不再是完全未见的最终样本外。可以作为回顾性验证，但不得称为封存测试。

真正证据来自：

- 历史滚动 walk-forward，每次训练仅使用当时已成熟数据。
- 多年份和多市场状态切片。
- 新版本冻结后的前瞻纸面运行。

### 6.2 阶段

#### Phase 0：合同冻结

固定版本、数据、PIT、成本、资金和报告字段；统一指标；建立三层仓位；标注每个控制的交易状态。

#### Phase 1：可比基线

重跑关闭主动替换后的 `v3` 和修复 schema 后的 `v3_ml`，唯一变量是 ML 融合。

#### Phase 2：候选漏斗消融

四项买入质量过滤逐项 on/off；比较前 10%、20%、30% 和仅按组合效用选择。先证明入场 Alpha 为正，再提高仓位。

#### Phase 3：退出锦标赛

按 E0→E1→E2→E3→E4 逐项加入，只保留跨窗口成本后增量为正的组件。

#### Phase 4：资金与集中度

比较 3/4/5 只、25%/35% 单仓软上限、固定/动态现金处理、贪心/一手组合优化。

#### Phase 5：赢家加仓

比较不加仓与盈利且 Alpha 持续时加仓，禁止混入亏损摊低。

#### Phase 6：股票池扩展

当前指数池对更广可交易 A 股池，同信号、同退出、同成本比较。

#### Phase 7：前瞻观察

冻结代码参数，至少观察 60 个交易日；交易过少则延长，不得为尽快通过而降低样本要求。

## 7. 准入门槛

### 7.1 进入下一研究阶段

初始预登记门槛：

- 汇总成本后净利润为正。
- 成本后 PF ≥ 1.15。
- 至少覆盖 24 个月、3 个市场状态切片和 150 笔已平仓交易；不足标记“证据不足”。
- 至少 60% 的主要时间切片净利润为正。
- 成本压力下 PF 不低于 1，或明确披露容量边界。
- 胜率路径或右尾路径至少一条成立，不要求两者同时高。
- 无 PIT、泄漏、执行、现金、库存和 NAV 完整性失败。

门槛只能根据训练/校准证据预登记修改，不得看最终窗口后追调。

### 7.2 实盘前

- 历史滚动验证通过。
- 回顾性窗口明确标注已污染。
- 前瞻纸面运行达到最低时间和交易样本。
- 实际券商成本已确认。
- 用户确认最大可损失金额和灾难冻结线。
- 运行中断、订单失败、涨跌停、停牌和数据缺失已演练。
- 能一键回滚，版本身份和输出目录不混淆。

## 8. 初始参数建议

| 参数 | 实验起点 |
|---|---:|
| 初始现金 | 20,000 元 |
| 现金缓冲 | 2,000 元，另测动态方案 |
| 最大持仓 | 5，3/4/5 做 A/B |
| 单股结构硬上限 | 40% |
| 单股研究软上限 | 25% 或 35% |
| 资本使用 | `allow_cash` |
| 主动替换 | 关闭 |
| 亏损加仓 | 关闭 |
| 赢家加仓 | 第一阶段关闭 |
| 最低佣金 | 按券商；未知时 0/1/5 元 |
| 回撤警报 | 35% |
| 新开仓冻结 | 45%，实盘前需确认 |
| PF 初始研究门槛 | 1.15 |
| 前瞻纸面观察 | 至少 60 个交易日 |

## 9. 实施与影响

| 末梢 | 预计文件 | 目的 |
|---|---|---|
| 版本身份 | `config.py`、`main.py`、`main_launcher_web.py` | 独立模式和研究标识 |
| 候选效用 | `mainline_v3.py` 或薄适配模块 | 成本、一手、组合边际效用 |
| 组合优化 | `retail_execution.py` 或独立纯函数 | 最多五只的一手组合搜索 |
| 三层仓位 | `runner.py`、`retail_execution.py` | 风险/期望/可执行仓位分离 |
| 简化退出 | `position_lifecycle.py`、`policy.py` | E0-E4 独立开关和原因码 |
| 成本敏感性 | `cost_model.py`、报告模块 | 最低佣金和压力测试 |
| 指标合同 | `runner_summary.py`、`research_gate.py` | 唯一盈利语义和小资金门禁 |
| 审计 | 候选、执行、组合、反事实报告 | 解释利润来自哪一层 |

优先扩展现有纯函数；只有职责不匹配时才新建模块，禁止复制平行账本或执行引擎。

```text
资金画像/成本合同
  → PIT股票池
  → 因子柜/ML排序
  → 硬过滤与软惩罚
  → 小资金候选效用
  → 一手组合优化
  → 三层仓位
  → 入场/持有/退出
  → 订单与成交
  → 现金/库存/NAV
  → 交易配对
  → 成本后盈利指标
  → 研究准入/Web展示
```

## 10. 禁止事项

- 不把 `factor_only` 政名后当特殊版。
- 不同时优化胜率、盈亏比、回撤、仓位和换手后挑最好结果。
- 不因允许大回撤而关闭数据、执行和账户安全。
- 不强制满仓或设最低持仓掩盖无信号。
- 不默认向亏损股票加仓。
- 不用最低佣金为 0 的结果代表真实收益，除非券商确认。
- 不把已反复查看的 2025–2026 窗口称为封存样本外。
- 不在 v3 与 v3_ml 对比时同时改变其他参数。
- 不因单次最终收益高就跳过多窗口、成本压力和前瞻观察。

## 11. 当前决策

SCAP-V1 只完成设计，不获得实盘交易权。实施顺序：

1. 指标唯一语义和版本隔离。
2. 三层仓位审计，先不改变交易。
3. 真实最低佣金与成本压力。
4. 关闭替换后的 v3/v3_ml 可比基线。
5. 入场软门槛消融。
6. 简化退出锦标赛。
7. 一手组合优化与集中度实验。
8. 赢家加仓和股票池扩展。
9. 冻结版本后做前瞻纸面观察。

只有前五步证明基础 Alpha 和退出后期望为正，才通过提高仓位、集中度或赢家加仓放大收益。
